/**
 * `/login` (AC-39, AC-45). **T40 SKELETON** — a heading and nothing else.
 *
 * No props: `next` comes from the URL (`?next=`, through `safeNext`), the request from `useLogin`,
 * and an already-authenticated visitor is sent on to `safeNext(next)` by the page itself.
 */
export function LoginPage() {
  return <h1>Log in</h1>;
}
