/**
 * The header's auth block (AC-42): `booting`, `anonymous`, `authenticated`, `unavailable`, each
 * distinguishable by role and text. **T40 SKELETON** — renders nothing.
 *
 * No props: everything comes from `useAuth()`.
 */
export function AuthStatus() {
  return null;
}
