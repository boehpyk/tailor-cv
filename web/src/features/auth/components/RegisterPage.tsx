/**
 * `/register` (AC-40, AC-45). **T40 SKELETON** — a heading and nothing else.
 *
 * No props: `next` comes from the URL, the request from `useRegister`.
 */
export function RegisterPage() {
  return <h1>Create account</h1>;
}
